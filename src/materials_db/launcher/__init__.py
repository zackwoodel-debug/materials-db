"""ModalFit launcher: a wrapper CLI, not a fork.

ModalFit's own source is never modified -- see modalfit_bridge.py's
docstring for how a pre-loaded launch is achieved by driving ModalFit's
existing, unmodified `model_predictor.py` module from the outside.
"""
